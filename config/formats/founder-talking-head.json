{
  "id": "founder-talking-head",
  "label": "Founder talking head",
  "mediaType": "video",
  "durationSecTarget": [30, 60],
  "script": "written by Claude Code from CLAUDE.md voice rules, one idea per script",
  "avatarPipeline": {
    "engine": "Higgsfield",
    "status": "closed, confirmed unsupported. Alex ran `higgsfield model list --video` on his own authenticated workspace: 30 video models, none of them avatar or lip-sync. Cross-checked independently: Marketing Studio's `avatars` command only registers a still-image reference (name + image), and the only generator that consumes it, `dtc-ads generate`, produces still ad images (quality/resolution/batch-size params, no duration or lip-sync param). Neither path in the CLI produces a talking, lip-synced video from a script. Do not re-open this without a new Higgsfield feature release."
  },
  "rules": [
    "Keep every script claim to something Alex would actually say and stand behind. It is his face and reputation.",
    "Real footage of Alex is the only path for this format now."
  ],
  "automatable": "no",
  "humanStep": "Alex films this one, real footage only. Per the original spec's own fallback rule, this format moves from a weekly slot to a monthly batch rather than staying on the weekly slate unfilled every week."
}
